version: '3.4'

services:
  tweeter_opentechiz:
    image: nguyenviethoa/basic-react:build-latest
    deploy:
      replicas: 1
      restart_policy: 
        condition: any
    ports:
      - 8099:4000
